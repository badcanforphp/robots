接口返回状态码释义：
（统一用四位数的状态码，返回格式为['code'=>xxxx]）
1、检测版本：
    6100 => 无需更新;
    6001 => 请更新到最新版本;
    6010 => 接口请求错误（传值方式有误）;
2、验证签名：
    4000 => 无参数传入;
    4001 => 签名不能为空;
    4002 => 签名不匹配（错误）;
    4003 => 无效wxid(token);